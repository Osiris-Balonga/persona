# Geographic data

## Country and territory registry

The V1 registry is a snapshot dated 2026-09-24 of the 249 assigned ISO 3166-1 alpha-2 codes. It also records the alpha-3 and numeric codes, an English name, a generation eligibility flag, and a telephone calling code when the source has one. The snapshot is regenerated with `npm run data:refresh:countries`; the two pinned source packages are development dependencies, so the API does not depend on a live external country service.

The assigned-code and English-name input is [`iso-3166` 4.4.0](https://github.com/wooorm/iso-3166), an MIT-licensed compilation. The [ISO 3166 Maintenance Agency](https://www.iso.org/iso-3166-country-codes.html) maintains the authoritative assignments; this project does not claim that the community package is an official ISO feed. The telephone input is [`libphonenumber-js` 1.13.14](https://github.com/catamphetamine/libphonenumber-js), which derives numbering metadata from [Google's libphonenumber](https://github.com/google/libphonenumber); the latter draws heavily on [ITU E.164 numbering information](https://www.itu.int/oth/T0202.aspx?parent=T0202). This is a dated mapping, not a promise that a prefix alone makes a valid or unassigned telephone number.

The snapshot has calling codes for 242 of the 249 ISO entries. `AQ`, `BV`, `GS`, `HM`, `PN`, `TF`, and `UM` have no entry in the pinned telephone metadata and carry `callingCode: null`. `PN` has a resident population but remains unavailable for beta profile generation until its local name pool is reviewed; its phone will remain `null` until a verified test range is available. Shared prefixes remain shared: for example, `US` and `PR` both have `+1`, while `CG` has `+242`.

The registry recognizes `AQ`, `BV`, `GS`, `HM`, `IO`, `TF`, and `UM` but marks resident-profile generation unavailable. These places lack a permanent resident population, so inventing a resident city or address would be misleading. Primary references include the [British Indian Ocean Territory profile](https://www.gov.uk/government/publications/british-indian-ocean-territory-knowledge-base-profile/british-indian-ocean-territory-knowledge-base-profile), the [UK overseas territories paper](https://assets.publishing.service.gov.uk/media/69b1718558d4ef5adaf0e46a/UK_Overseas_Territories_Information_paper.pdf), [Australian Antarctic information on Heard Island](https://www.antarctica.gov.au/about-antarctica/history/stations/heard-island-and-mcdonald-islands/), and the [French overseas ministry's TAAF profile](https://www.outre-mer.gouv.fr/territoires/terres-australes-et-antarctiques-francaises). An eligible code means it can enter the later data and generation pipeline; it does not assert full local name, city, address, or portrait coverage today.

Of the 242 resident-eligible codes, beta profile generation currently permits all 58 African codes, 50 European codes, 40 Asian codes, 31 North American and Caribbean codes, 16 Oceanian codes, and all 14 South American codes: 209 codes in total. Their source-backed local name pools have been reviewed for the generated examples; some remain small compatibility samples. The remaining 33 codes stay recognized in the registry and geographic audit, but profile generation is pending a local name review. Country selection without a `country` filter draws only from the available set. This review does not claim complete representation of every naming tradition within an available country.

An unknown code, an uninhabited code, or a resident code pending name review returns HTTP 400 with `UNSUPPORTED_VALUE` and `parameter: "country"`. Malformed country syntax remains `INVALID_QUERY`. The `profileGeneration` value in the coverage matrix distinguishes `available`, `pending-name-review`, and `unavailable`; the registry's `generation` flag continues to describe permanent-resident eligibility.

## City sample and coverage

The city snapshot uses [GeoNames `cities1000.zip`](https://download.geonames.org/export/dump/cities1000.zip) and [admin1 names](https://download.geonames.org/export/dump/admin1CodesASCII.txt), extracted on 2026-09-24 under [CC BY 4.0](https://www.geonames.org/about.html). Credit: **GeoNames**. Source hashes and editions are stored in `src/geography/sources.ts`. For each resident-eligible ISO code, the build keeps up to twelve most populous named populated places and adds the capital if it was outside that set. It removes duplicate city names within a code. Each city records coordinates and an administrative region when the source has one. `src/geography/city-data.ts` is a checked-in snapshot; `npm run data:refresh:cities -- <cities1000.zip> <YYYY-MM-DD> <admin1CodesASCII.txt>` rebuilds it from downloaded extracts. The source's [field definitions and inclusion criteria](https://download.geonames.org/export/dump/readme.txt) should be reviewed before updating the snapshot. A refresh must include new source hashes, provenance review, and geographic validation checks.

This is a bounded city sample, not a complete gazetteer. All 242 resident-eligible codes have at least one locality in the snapshot. Seven territories without permanent residents have none. A city filter must resolve against the selected country's sample; an unknown or mismatched city must fail rather than silently switch countries. GeoNames data is supplied as is, so the sample needs country-batch review before public beta use.

`src/geography/coverage.ts` produces one coverage row per assigned ISO code. Each category records an ingestion status, source, fallback, and review state. `pending` means the API must not claim that category is ready for the code. `automated` means schema and provenance checks ran; it does not mean human review. The checked-in source manifest in `src/geography/sources.ts` records source, usage right, edition, and GeoNames archive hashes. `validateGeographicData()` rejects missing eligible cities, duplicate identifiers, invalid codes, and ingested data without a known source and usage right. Future address, distribution, and portrait batches must add their own source and coverage entries before they can be marked ingested. Regional fallbacks must be explicit in the corresponding coverage cell.

## Names and appearance

For broader name coverage, `npm run data:candidates:names -- --countries=GH,SN,RW --output=<report.json>` collects country-specific candidate components from [Wikidata Query Service](https://query.wikidata.org/) using citizenship (`P27`), sex or gender (`P21`), given name (`P735`), and family name (`P734`). [Wikidata structured data is CC0](https://www.wikidata.org/wiki/Wikidata:Licensing). The report records each country's queries, retrieval time, and a SHA-256 digest of its aggregate source rows. It contains only name-component counts, never person records. Each completed or failed country is saved immediately; rerunning the same command skips completed countries and retries failures. The command exits unsuccessfully while any country has failed. The [public query service](https://www.mediawiki.org/wiki/Wikidata_query_service/User_Manual) limits queries to 60 seconds and may throttle clients, so failures must stay visible rather than be silently treated as empty countries.

The collector stops after two consecutive country failures, leaving unattempted codes pending for the next run. The report is research input and is kept outside the runtime catalog. Counts measure Wikidata coverage, especially public-figure coverage, not population frequency. An editor must inspect local plausibility, spelling, gender ambiguity, and second-name usage before moving a country into the checked-in catalog. Sparse results stay sparse; names from another country are not added to meet a target count. The current small reviewed pools are compatibility samples and need this broader review before the public beta.

If WDQS is unavailable, rerun the same command with `--engine=qlever`. [Wikidata lists QLever as an alternative endpoint](https://www.wikidata.org/wiki/Wikidata:SPARQL_query_service/Alternative_endpoints); its [usage information](https://github.com/ad-freiburg/qlever/wiki/Usage-and-privacy-information-for-the-QLever-SPARQL-endpoints) applies. QLever queries use English Wikidata labels explicitly, so an entity without an English label will be absent from that candidate report. The per-country `source` and query fields identify which endpoint produced each result, including when a resumed report combines endpoints. Do not compare the counts from the two engines as if they were population statistics.

For codes where citizenship does not identify the territory, `npm run data:candidates:names -- --countries=DJ,EH,ER,GW,KM,LY,MR,NE,SH,ST,SZ,TD,YT --engine=qlever --basis=birthplace --output=data/geography/africa-birthplace-candidates-2026-09-24.json` collects a separate aggregate report using Wikidata birth place `P19` and administrative hierarchy `P131*`. The pinned snapshot SHA-256 is `a42dceddd27373a2ce32353ccb26582992e6b5718f91aa48a8b3b826fd26fc58`. All 13 queries completed; only `LY` reached 50 raw entries in every field. Birth place is not evidence of current residence, citizenship, or local name frequency, so these candidates require independent review before use.

The 2026-09-24 Africa research snapshot at `data/geography/africa-name-candidates-2026-09-24.json` was collected uniformly through QLever. It covers all 58 Africa batch codes, has no failed query, and has SHA-256 `db878f5a1f9b4b1ca9165cc3a2b9b4938e9090866410973dd5ea7a524afa89d6`. Run `npm run data:audit:names` for per-code candidate counts, rejected labels, gender ambiguity, first/second-name overlap, format warnings, endpoint, and source hash. Forty-four codes have at least 50 candidates in each field; `DJ`, `EH`, `ER`, `GW`, `KM`, `LS`, `LY`, `MR`, `RE`, `SH`, `ST`, `SZ`, `TD`, and `YT` are below that threshold. The snapshot is a review queue, not a runtime pool or a country-readiness decision. Citizenship codes are especially weak for some territories, and missing English labels may reduce coverage further; those cases require another local source instead of padding from an unrelated country.

The Europe research snapshot at `data/geography/europe-name-candidates-2026-09-24.json` was collected with the same citizenship-based QLever query on 2026-09-24. Its SHA-256 is `170863c630bde60968af7b6d125083144bd735aeec08b9b66ef002186a018e5a`. All 52 requested codes completed; 43 have at least 50 candidate components in each field. `AX`, `DK`, `FO`, `GG`, `GI`, `IM`, `JE`, `SJ`, and `VA` need other sources or a carefully documented smaller pool. The report is research input only: citizen names do not establish a national frequency distribution, gendered surnames still need review, and the sparse territories must not inherit another country's pool silently.

The second Europe name batch reviews `BG`, `BY`, `CY`, `CZ`, `GR`, `IS`, `LT`, `LV`, `MK`, `PL`, `RU`, `SK`, and `UA`. Its family-name candidate snapshot at `data/geography/europe-gendered-family-candidates-2026-09-24.json` groups Wikidata `P734` labels by a documented person's citizenship and `P21` gender; SHA-256 `28641da72c5b85e1c9a5ae1d2b3170f6862fed88596cd8be9b88cf3af44ca62c`. Selected female and male family components are stored separately, alongside 50 reviewed given components per gender. The review removed visibly mismatched forms and labels, then checked top, middle, and tail combinations for all 13 codes. [Czech civil-registry guidance](https://mv.gov.cz/clanek/prijmeni-zen.aspx) describes the common feminine suffix, and [Iceland's registry](https://island.is/en/name-giving/icelandic-naming-convention-surnames) describes the `-son` and `-dóttir` pattern; the compatibility pools reflect those examples without claiming that every citizen uses them. The selected pools are biased toward documented people, and randomly recombined given and family components cannot represent all language communities. Illustrative addresses were checked for each code; before synthetic street generation, 12 had partial coverage and `IS` was marked ingested. All 13 now have partial coverage because their street lines are synthetic. At that stage, nine European codes remained pending name review.

The third Europe batch reviews `AX`, `DK`, `FO`, `GG`, `GI`, `IM`, and `JE` with the birth-place QLever snapshot at `data/geography/europe-birthplace-candidates-2026-09-25.json` (SHA-256 `5c6e4e2d92bc17520f23959f4a47ca0e24eb039996fb2eaca3cee797a84cd9b2`). This is a source of territory-linked candidate components, not proof that a person still resides there. Its candidate counts were sufficient for bounded local pools in seven codes after checking field placement, gender, and the top, middle, and tail samples. The smaller Faroe pool adds given-name facts from [Statistics Faroe Islands' 2024 resident rankings](https://hagstova.fo/fo/tidindi/jakup-og-maria-vanligastu-novnini-i-foroyum). Before synthetic street generation, six reviewed address examples were partial and Gibraltar's was ingested. All seven now have partial coverage because their street lines are synthetic. The illustrative `SJ` and `VA` addresses were also reviewed and remain partial. `SJ` and `VA` remain `pending-name-review`: neither has birth-place candidates in this snapshot, and their other country-linked results do not establish a coherent female, male, and second-name pool. They are recognized ISO codes but are excluded from profile selection until reviewed local evidence exists.

The Asia research snapshot at `data/geography/asia-name-candidates-2026-09-25.json` covers all 50 codes in the Asia review issue using the same citizenship-based QLever query. Its SHA-256 is `d6e92036ef467d5e5ba43c767a9a15e5914857c489c882b3f06b932644868ca8`. Thirty-eight codes have at least 50 raw female, male, and family-name labels; `BN`, `BT`, `CC`, `KH`, `LA`, `MN`, `MO`, `MV`, `OM`, `QA`, `TJ`, and `TM` are below that threshold. This is a review queue, not the runtime catalog: the labels still require checks for script, naming order, gender, source-country fit, and two-field form compatibility. Sparse codes need official or local supplementary evidence rather than a shared-language fallback.

The first Asia runtime batch reviews `AM`, `BD`, `GE`, `IL`, `IN`, `IR`, `LK`, `NP`, `PH`, `PK`, and `TR` from that pinned snapshot. Each has 50 female given components, 50 male given components, and 75 second-name components after screening the ranked fields and excluding obvious prefixes or mislabeled personal names. Top, middle, and tail recombinations and an illustrative address per country were checked. These are compatibility pools drawn from documented citizens, not population frequencies; `lastName` may denote a second personal or paternal component in some naming systems. [HM Passport Office's India profile](https://www.gov.uk/government/publications/india-knowledge-base-profile/india-knowledge-base-profile) describes several regional and religious naming conventions, so the India pool cannot represent all of them. Eight address coverage cells remain partial. Missing verified postcodes remain `null`, and dangling separators in Bangladesh and Philippines examples are removed when optional components are absent.

The second Asia batch reviews `AE`, `AF`, `BH`, `IQ`, `JO`, `KW`, `LB`, `PS`, `SA`, `SY`, and `YE` from the same snapshot, with 50 female, 50 male, and 60 second-name components per code, except Bahrain (`BH`), which uses a tighter locally coherent selection of 40 female, 50 male, and 35 family components. Whole-person labels, lone prefixes, dynasty or tribe labels, and obvious malformed candidates were removed before top, middle, and tail samples were recombined. The two-field result is a compatibility display form; many Arabic naming systems can have additional patronymic components, and the lists do not claim national frequency or complete community coverage. [HM Passport Office's UAE guidance](https://www.gov.uk/government/publications/united-arab-emirates-knowledge-base-profile/united-arab-emirates-knowledge-base-profile) describes a family surname in a longer name, and its [Kuwait guidance](https://www.gov.uk/government/publications/kuwait-knowledge-base-profile/kuwait-knowledge-base-profile) discusses abbreviated patronymic components. All 11 illustrative addresses remain partial, usually because street wording is generic or no verified city-linked postcode exists. A city matching its displayed region is shown once in a format without a separate city field.

The third Asia batch reviews `CN`, `HK`, `JP`, `KP`, `KR`, `TW`, and `VN` from the pinned snapshot. After inspecting ranked labels, the selected pools contain 38–50 given components per gender and 40–75 family components. We removed `Thị` as a standalone Vietnamese given name, out-of-context Korean labels, misplaced surname labels, and some English labels from the mainland China selection. English given names remain in the Hong Kong and Taiwan samples, where they occur in the source, but the snapshot is not a population-frequency sample. [HM Passport Office's Japan profile](https://www.gov.uk/government/publications/japan-knowledge-base-profile/japan-knowledge-base-profile) distinguishes given and family names; [UK judicial guidance](https://valuationtribunal.gov.uk/app/uploads/2021/11/Equal-Treatment-Bench-Book-February-2021-1.pdf) notes traditional family-first ordering in Chinese names. Persona keeps `firstName` as the given component, `lastName` as the family component, and `fullName` in its existing given-then-family API order for form compatibility. It does not promise a locally formatted legal name. The seven illustrative addresses remain partial; repeated city/region labels and adjacent labels without a separator were corrected.

The address review also covers the 21 Asia codes whose addresses had not previously been marked reviewed: `AZ`, `BN`, `BT`, `CC`, `ID`, `KG`, `KH`, `KZ`, `LA`, `MM`, `MN`, `MO`, `MV`, `MY`, `OM`, `QA`, `SG`, `TH`, `TJ`, `TM`, and `UZ`. The review used one generated city-linked example per code and checked the source city and displayed region. It removed Azerbaijan's `AZ` postcode prefix when the city has no verified postcode. These address decisions do not change name-readiness: 10 of the 21 codes still need a locally coherent name pool. Missing postcodes remain `null`, and partial format/street fallbacks remain explicit in the coverage matrix.

The fourth Asia name batch reviews `AZ`, `KG`, `KZ`, and `UZ`. The pinned citizenship snapshot supplies 50 female and 50 male given components per code. Family components are separated by attested masculine and feminine suffixes rather than mixed; the smaller female pools contain 13–28 names and are deliberately not padded. [HM Passport Office's Azerbaijan profile](https://www.gov.uk/government/publications/azerbaijan-knowledge-base-profile/azerbaijan-knowledge-base-profile) describes gendered family names, while its [Kazakhstan profile](https://www.gov.uk/government/publications/kazakhstan-knowledge-base-profile/kazakhstan-knowledge-base-profile) also records patronymic and Kazakh suffix conventions. The two-field API samples cover only the suffix forms selected here. Wikidata citizenship overrepresents documented public figures and, for `KG`, `KZ`, and `UZ`, Russian-style names; this is not a population model. The four address examples remain partial.

The fifth Asia batch reviews `ID`, `SG`, `TH`, `MV`, and `QA`. Its source-ranked candidates were screened for wrong field placement, dynasty labels, and obvious foreign or non-person labels. The Thai pool contains 35 female, 35 male, and 34 family components selected from Thai-looking entries; Singapore keeps its documented mix of English given names and Chinese family names. The Maldives and Qatar selections are smaller but avoid the unsuitable labels found in the raw tail. The Indonesian `lastName` is explicitly a *second display component*, not a claimed inherited surname: [HM Passport Office's Indonesia profile](https://www.gov.uk/government/publications/indonesia-knowledge-base-profile/indonesia-knowledge-base-profile) says Indonesian legal names do not follow a universal given/middle/family split. All five address examples remain partial.

The remaining ten Asia codes are recognized and their address examples are reviewed, but profile generation stays `pending-name-review`:

| Code | Raw female / male / family labels | Remaining name-review gap |
| --- | ---: | --- |
| `BN` | 28 / 60 / 45 | Female and second-name fields mix communities and titles; needs a coherent local sample. |
| `CC` | 0 / 1 / 1 | Citizenship query provides no usable female pool; seek island-linked evidence. |
| `KH` | 33 / 78 / 115 | Female candidates include many foreign labels; Khmer first/second components need validation. |
| `LA` | 11 / 30 / 28 | Too few locally plausible female and second components. |
| `MN` | 32 / 78 / 92 | Given/patronymic field placement and gender need a Mongolian source review. |
| `MO` | 8 / 12 / 24 | Citizenship coverage is sparse for Macau; seek territory-linked names. |
| `MY` | 200 / 200 / 200 | Ample raw labels, but Malay, Chinese, and Indian name components should be paired within coherent community pools. |
| `OM` | 23 / 84 / 27 | Female and family labels are sparse and include obvious non-Omani candidates. |
| `TJ` | 44 / 112 / 113 | Need attested feminine family forms and a balanced Tajik sample. |
| `TM` | 26 / 86 / 69 | Female and family labels are sparse; gendered and Turkmen forms need review. |

The raw counts are Wikidata coverage, not a popularity ranking. None of these ten codes silently borrows a neighbouring country's pool for beta profile generation.

The North American and Caribbean review covers all 41 resident-eligible codes in that region. The citizenship candidate report at `data/geography/north-america-name-candidates-2026-09-25.json` (SHA-256 `ff94dd0962bec588d768d6ccafc42bfaeff3cd92a7c34e223c80335d7c82271a`) completed all 41 QLever queries. The birth-place report at `data/geography/north-america-birthplace-candidates-2026-09-25.json` (SHA-256 `3204de1275952be70033181a5b2073f4bfe3dd9dca15df97294823ff62c9d35a`) completed 19 territory-focused queries. Both contain aggregate [Wikidata CC0](https://www.wikidata.org/wiki/Wikidata:Licensing) name components, not person records or population frequencies. The country-linked pools in `src/geography/north-america-reviewed-names.ts` make 24 codes available. The birth-place-linked pools in `src/geography/north-america-territory-names.ts` make `AW`, `CW`, `GL`, `GP`, `MQ`, `PR`, and `VI` available. Ranked labels and generated combinations were screened for field placement, gender, and obvious country mismatch; the remaining pools are bounded examples of two-field names. [Greenland's language secretariat](https://oqaasileriffik.gl/en/personal-names/) and [HM Passport Office's Guatemala profile](https://www.gov.uk/government/publications/guatemala-knowledge-base-profile/guatemala-knowledge-base-profie) informed checks of naming conventions. A birth place does not establish present residence, and a randomly combined name does not establish a particular community or frequency.

All 41 illustrative city-linked addresses were reviewed. Their street names are visibly fictional and many country formats retain an explicit partial fallback. Missing verified postcodes stay `null`; the absent Haiti postcode no longer leaves a dangling `HT` prefix. The sampled San José capital in Costa Rica no longer inherits `40602`, which belongs to a different San José locality in Heredia according to [Costa Rica's statistical geography](https://sistemas.inec.cr/pad5/index.php/catalog/374/variable/F4/V254?name=DISTserv). A postal code for that capital remains `null` until a locality-matched source is available. This review checks sample coherence, not postal deliverability.

Ten codes remain recognized but `pending-name-review`: `AI`, `BL`, `BQ`, `KY`, `MF`, `MS`, `PM`, `SX`, `TC`, and `VG`. Their territory-linked female candidate counts are respectively 5, 2, 0, 15, 6, 12, 6, 8, 3, and 5, too sparse to establish a coherent female, male, and second-name pool after screening. Each code has an explicit review note in the coverage matrix, and none borrows another territory's pool for generation. Their addresses were reviewed independently of name readiness.

The Oceania citizenship candidate report at `data/geography/oceania-name-candidates-2026-09-25.json` covers all 27 regional codes (SHA-256 `3fa9a18df28904f3fadc5cccf02d5b6e90c8ed9335741793dcada3cdd5fa2f91`). The birth-place report at `data/geography/oceania-birthplace-candidates-2026-09-25.json` covers 21 sparse codes (SHA-256 `1d53617a2bf92d3af624e70dd978a43aeef91131a8ec81bd7ded656d4b5be37e`). Both reports completed every query and contain only aggregate [Wikidata CC0](https://www.wikidata.org/wiki/Wikidata:Licensing) name components. The reviewed citizenship pools make `AU`, `CK`, `FJ`, `FM`, `NR`, `NZ`, `PG`, `PW`, `SB`, `TL`, `VU`, and `WS` available. Birth-place-linked pools make `AS`, `GU`, `NC`, and `PF` available. Selected fields were screened for misplaced names and obvious country mismatches; a few misleading labels were removed. These are small compatibility samples drawn from documented people, not population frequencies or a claim to represent all communities. The [US Department of Education's Pacific Islands naming guide](https://ies.ed.gov/ncee/rel/infographics/pdf/REL_PA_Why_Do_Student_Names_Matter.pdf) describes variation in names across island cultures, so Persona does not infer one regional naming scheme or borrow a neighbouring island's pool.

All 27 Oceanian city-linked address examples were reviewed, including the 11 codes whose names remain pending. Missing verified postcodes remain `null`, and partial country formats retain an explicit fallback in the coverage matrix. Sydney's sample now uses the [Australia Post delivery-area postcode 2000](https://auspost.com.au/postcode/sydney-2000) instead of the GeoNames match `1001`, which is unsuitable for its fictional street line. The Pitcairn postcode `PCRN 1ZZ` follows the [UPU's addressing example](https://www.upu.int/UPU/media/upu/PostalEntitiesFiles/addressingUnit/pcnEn.pdf). The address samples illustrate format and locality; they are not deliverable addresses.

`CX`, `KI`, `MH`, `MP`, `NF`, `NU`, `PN`, `TK`, `TO`, `TV`, and `WF` remain `pending-name-review`. Candidate counts for women are especially sparse or fail local field checks, and `CX` has no country- or birth-place-linked candidates. Pitcairn has a verified postcode but too few male and second-name candidates to support a reusable local pool. The per-code reasons are recorded in `src/geography/coverage.ts`; profile selection excludes these codes until a separate source review succeeds.

The South America citizenship candidate report at `data/geography/south-america-name-candidates-2026-09-25.json` covers all 14 regional codes (SHA-256 `eed673c6594b61d1c9bd4df671600d11b714afeafe2085dc81323ea2b25ed152`). Its country-linked pools make `AR`, `BO`, `BR`, `CL`, `CO`, `EC`, `GY`, `PE`, `PY`, `SR`, `UY`, and `VE` available. The birth-place report at `data/geography/south-america-birthplace-candidates-2026-09-25.json` (SHA-256 `7c25edcc56b820f7c20880897ae94ddb6724b2cebf8ef6380a515efc35f00a2e`) makes `FK` and `GF` available with smaller reviewed pools. Both completed all queries and contain aggregate [Wikidata CC0](https://www.wikidata.org/wiki/Wikidata:Licensing) name components. Ranked given and second-name fields and generated combinations were screened; a malformed male candidate in Peru was excluded. Citizenship or birth place is not a national name-frequency measure. Persona keeps `firstName` and `lastName` as two fields for developer forms, without claiming that all local legal names use one given name and one surname; [Argentina's passport guidance](https://www.gov.uk/government/publications/argentina-knowledge-base-profile/argentina-knowledge-base-profile) and [Brazil's guidance](https://www.gov.uk/government/publications/brazil-knowledge-base-profile) describe additional name components in some cases.

All 14 illustrative city-linked South American addresses were reviewed. The [UPU's Falkland address guide](https://www.upu.int/UPU/media/upu/PostalEntitiesFiles/addressingUnit/flkEn.pdf) specifies the territory-wide `FIQQ 1ZZ` postcode, now included in the Stanley example. Brazil's [postal guide](https://www.correios.com.br/enviar/precisa-de-ajuda/guia-de-enderecamento/guia-de-enderecamento) and [IBGE state abbreviations](https://www.ibge.gov.br/biblioteca/visualizacao/instrumentos_de_coleta/doc2311.pdf) support city–UF display such as `São Paulo-SP`. GeoNames values ending in `-000` in the sampled Brazilian cities are range starts rather than evidence for a fictional street, so the example leaves the postcode `null`. Remaining missing postcodes and partial country formats stay explicit in coverage; these examples are not deliverable addresses.

The name snapshot combines [GeoNames `countryInfo.txt` language tags](https://download.geonames.org/export/dump/countryInfo.txt) (CC BY 4.0; credit: **GeoNames**) with the [MIT-licensed person locale data in `@faker-js/faker` 10.6.0](https://github.com/faker-js/faker). It is regenerated with `npm run data:refresh:names -- <countryInfo.txt>` after the source hash is reviewed and updated. The runtime uses the committed snapshot, not the source packages or a remote service. Each eligible code has one or more named pools. An exact country locale is labelled `local`; a shared language locale is `language`; `en` is the explicit global fallback when no listed language has a supported name pool. When country-specific pools exist, selection uses only those pools; otherwise it uses the supported language pools. The first listed language has editorial weight 4, the second 2, and later supported languages 1 each. These weights are a reproducible selection rule, **not demographic estimates**. All pool weights and nonlocal fallbacks appear in the source data and coverage matrix. A sampled name may still be culturally inappropriate; country-batch review remains necessary before beta release.

The V1 appearance vocabulary is `west-african`, `central-african`, `east-african`, `southern-african`, `north-african`, `middle-eastern`, `european`, `south-asian`, `east-asian`, `southeast-asian`, `latin-american`, and `mixed`. These are broad visual catalog labels, not nationality, ethnicity, or a reliable inference about a person. Name pool selection takes the country and gender but never the appearance or portrait. A requested country and appearance can be combined independently. The portrait catalog will separately validate compatible images; no image is generated by a request.

The Georgian `ka_GE` Faker locale has only a generic given-name list, which mixed male and female names for either requested gender. The snapshot now uses ten given names per gender from [Public Service Development Agency counts reported in August 2024](https://rustavi2.ge/ka/news/289258); the [agency also publishes name statistics](https://sda.gov.ge/en/home/). These are public name-frequency facts, not copied profiles or demographic probabilities. Georgian family names remain from the pinned Faker locale. The small curated pool prevents an obvious gender mismatch but is not a claim of complete Georgian name coverage.

[HM Passport Office guidance for Myanmar](https://www.gov.uk/government/publications/myanmar-burma-knowledge-base-profile/myanmar-burma-knowledge-base-profile) says a Burmese name's components are generally given names, and a family surname is not always used. Myanmar therefore uses a small, gender-labelled local pool of complete given names from the [published male](https://burmesenames.wordpress.com/2014/06/07/the-100-most-popular-male-burmese-names/) and [female](https://burmesenames.wordpress.com/2014/06/07/the-100-most-popular-female-burmese-names/) common-name counts. Only six names per gender are retained; no directory entries, contact details, frequency counts, or names scraped from identifiable profiles are included. The counts derive from an older telephone directory with [urban, age, and gender biases](https://burmesenames.wordpress.com/about-the-burmese-name-data/), so this is a narrow compatibility pool, not representative coverage. The local pool replaces the former global English fallback for `MM`.

[HM Passport Office guidance for Indonesia](https://www.gov.uk/government/publications/indonesia-knowledge-base-profile/indonesia-knowledge-base-profile) says name components are treated as one given name and a family surname is not legally recognized. For `ID`, the seeded Faker `id_ID` components provide two display fields. For `MM`, the final word of a selected multiword name is used as `lastName`, preserving the complete name. These fields support common developer forms; `lastName` does not promise an inherited surname. `npm run data:refresh:names` refreshes the Faker snapshot but does not overwrite the curated Myanmar pool.

Andorra's [2013 civil registry report](https://www.govern.ad/ca/w/el-registre-civil-publica-la-memoria-de-funcionament-2013-1) supplied the initial six given-name examples per gender. The wider Andorran citizenship-based Wikidata candidate pool now retains those examples and adds locally associated given and second-name components. The former Spanish-language family-name fallback is no longer used.

The first Europe review batch opens `AD`, `AL`, `AT`, `BA`, `BE`, `CH`, `DE`, `EE`, `ES`, `FI`, `FR`, `GB`, `HR`, `HU`, `IE`, `IT`, `LI`, `LU`, `MC`, `MD`, `ME`, `MT`, `NL`, `NO`, `PT`, `RO`, `RS`, `SE`, `SI`, and `SM`. Each has at least 50 female and 50 male given-name components and 100 second-name components from the pinned Europe citizenship snapshot; Andorra additionally retains the earlier civil-registry examples. The selected lists use Latin-script labels to avoid mixing transliterations and scripts within one generated name. Obvious misclassified male `Maria` entries were removed for Liechtenstein, the Netherlands, and San Marino. Top, middle, and tail candidates and one generated profile per gender and code were checked; these are documented-person compatibility pools, not national name-frequency samples. The beta still requires local review of 22 other European codes, including gender-sensitive surname forms and territories with few citizenship candidates.

A city-linked fictional address was inspected for each of the 30 newly available codes. At that stage four had ingested address coverage and 26 had an explicit partial fallback; all 30 now have partial coverage because their street lines are synthetic. A missing verified postcode now also removes a stranded country prefix such as `HR-`, `L-`, or `MD-` from the formatted address; a hyphen attached to the city is removed without dropping the city. The review flag describes this bounded example check, not real-world deliverability.

For Bhutan, [Karma Phuntsho's cultural account](https://texts.mandala.library.virginia.edu/shanti_texts/node_embed/39441) describes a common two-part given name, usually with a gender-neutral first part and a second part that often signals gender, without an inherited family surname. The V1 pool combines a few name elements from that account and exposes them as `firstName` and `lastName` for two-field forms. It covers one documented naming convention, not every Bhutanese community. It replaces the former global English pool.

For Malawi, the [Peace Corps *Learning Chichewa* handbook](https://eric.ed.gov/?id=ED206157) supplies a small set of given names explicitly described as suitable for girls and boys. The [IFLA *Names of Persons* guide](https://www.ifla.org/g/cataloguing/names-of-persons/) supplies examples of Malawian clan names. Persona recombines five given names and four clan names rather than copying complete names from identifiable people. This local compatibility pool removes the global English name fallback for `MW`; it is not a national frequency sample or a review of Malawi's other naming traditions. Its sample has passed the initial name review; the broader Africa batch remains open.

For Ethiopia, [HM Passport Office guidance](https://www.gov.uk/government/publications/ethiopia-knowledge-base-profile/ethiopia-knowledge-base-profile) describes a personal name followed by the father's given name. The small [Tesfa Alliance Ethiopian name list](https://tesfaalliance.org/names.php) supplies separate male and female examples. Persona selects a personal name and a distinct male given name for the second display field; it does not copy complete names from identifiable people. This removes the English Faker fallback for `ET`, but the sample mainly reflects Amharic naming and does not represent all Ethiopian languages or naming customs. Its sample has passed the initial name review; the broader Africa batch remains open.

The first Africa review batch added narrow local compatibility pools for `CG`, `GH`, and `SN`. Congo uses name components from the [Republic of the Congo Senate's public member roster](https://www.senat.cg/groupe-parlementaire-pct-et-allies.php); [HM Passport Office](https://www.gov.uk/government/publications/congo-republic-of-the-knowledge-base-profile/congo-republic-of-the-knowledge-base-profile) documents the forename and surname structure. Ghana and Senegal originally used the pinned Faker `en_GH` and `fr_SN` locales, checked against [Ghana's parliamentary member directory](https://www.parliament.gh/members) and [Senegalese presidency appointment notices](https://www.presidence.sn/fr/actualites/conseil-2/). Their pools now contain 50 female names, 50 male names, and 50 second names for Ghana; 50 female names, 51 male names, and 51 second names for Senegal. They combine the first 50 distinct aggregate candidates from [Wikidata Query Service](https://query.wikidata.org/) for each field with distinct earlier reviewed components. The candidate report was retrieved on 2026-09-24 and has SHA-256 `da3dec79e6ec5a0f412b9d94f9360712fa74cff856cca7be56440f9d0afff503`; its selection query is recorded in `scripts/collect-wikidata-name-candidates.mjs`. The top, middle, and tail of each selected list were inspected for name form and country plausibility. The lists store name components rather than complete source records. Wikidata counts reflect its contributor and public-figure coverage, not national frequency or every language community. Regional address and wider name review remain open.

South Africa uses the top male and female first names and surnames in [Statistics South Africa's Recorded Live Births 2024, Table 12](https://www.statssa.gov.za/publications/P0305/P03052024.pdf). Some given names occur in both sex columns, so they are retained in both lists. This small recent-birth sample is an explicit source-backed compatibility pool; it is not a name distribution for all ages, languages, or communities in South Africa.

Rwanda's second display component draws from the [National Institute of Statistics' 2024 recorded baby surnames](https://www.statistics.gov.rw/sites/default/files/documents/2025-04/Rwanda%20Vital%20Statistics%20Report%202024_N.pdf); given-name examples were checked in the [Parliament of Rwanda's public deputy profiles](https://www.parliament.gov.rw/chamber-of-deputies-2/member-profile/deputies-profiles). A Rwandan surname is not necessarily inherited in the Western sense. Uganda's original sample used given and second-name components from the [Parliament of Uganda's public member roster](https://mpsdb.parliament.go.ug/), with women's representative roles supporting the female examples. These components support two-field developer forms; the expanded pools are not national frequency or age distributions.

The next Africa name batch adds Angola (`AO`), Burkina Faso (`BF`), Benin (`BJ`), and DR Congo (`CD`), each with 50 female, 50 male, and 50 second-name components. Rwanda (`RW`) expands to 51, 54, and 55 respectively, retaining distinct components from its earlier reviewed pool. The candidates were selected from 2026-09-24 [Wikidata Query Service](https://query.wikidata.org/) aggregate reports. The report hashes are `5ee1d1ce7fcd7550d336034d027550ca679b95190983c26b460266c58c4891be` for `AO/BF/BJ/CD` and `da3dec79e6ec5a0f412b9d94f9360712fa74cff856cca7be56440f9d0afff503` for `RW`; selection uses the country-citizenship, name-component, and sex-or-gender query documented above. Top, middle, and tail entries were checked for field placement, spelling, and local plausibility; obvious malformed or misplaced candidates were excluded. The generated first and second fields are for common developer forms, not a claim that every person has an inherited family surname. Example city-linked addresses for Luanda, Ouagadougou, Cotonou, Kinshasa, and Kigali were checked with missing postcodes left `null`. These bounded lists still overrepresent Wikidata's documented people and are not national name-frequency distributions.

The following reviewed batch opens Côte d'Ivoire (`CI`), Cameroon (`CM`), Guinea (`GN`), Mali (`ML`), and Togo (`TG`). Each has 50 female, 50 male, and 50 second-name components selected from the pinned 2026-09-24 QLever candidate snapshot above. The most frequent, middle, and least frequent selected components were checked for field placement, gender, spelling, and plausible country association. Obvious swaps and stray foreign labels were removed, including male given names in female lists and surnames entered as given names. Generated samples and illustrative addresses were checked for Abidjan, Douala, Conakry, Bamako, and Lomé; missing postal evidence remains `null`. These lists support two-field forms but do not describe the population frequency or all naming communities in any of the five countries.

The same snapshot supports a further reviewed West Africa batch for Gambia (`GM`), Liberia (`LR`), Nigeria (`NG`), and Sierra Leone (`SL`), each with 50 components per field. Candidate lists were inspected for gender and field placement; malformed organization labels and obvious cross-field errors were excluded. Liberia's English-language surnames reflect its documented naming history, while Nigerian components include several local and religious naming traditions. Generated first/second-name and city-linked address samples were checked for Serekunda, Monrovia, Lagos, and Freetown. These lists remain bounded compatibility pools; they do not imply national frequencies, exclusive gender usage, or complete representation of local communities. Niger (`NE`) remains pending despite a raw count above 50 because its female candidate list contains male given names and a place label.

The North Africa review adds Algeria (`DZ`), Egypt (`EG`), Morocco (`MA`), and Tunisia (`TN`) from the same pinned QLever snapshot, with 50 female, 50 male, and 50 second-name components each. Entries were inspected across each ranked field; the Egyptian candidate `makarona`, a stray `Zeynep`, an Arabic-script-only surname among Latin-script labels, and malformed surname labels were omitted. Top, middle, and tail generated names and illustrative addresses were checked for Algiers, Cairo, Casablanca, and Tunis; unavailable postcodes remain `null`. These are bounded, Latin-script compatibility pools rather than population-frequency data or an exhaustive account of the region's languages and naming traditions.

The island and southern batch adds Cape Verde (`CV`), Equatorial Guinea (`GQ`), Madagascar (`MG`), and Mozambique (`MZ`) from the pinned QLever snapshot, again with 50 components per field. Obvious cross-field labels, whole-person names mistakenly classified as a second-name component, and unrelated surnames were removed. Top, middle, and tail generated names and illustrative addresses were checked for Praia, Bata, Antananarivo, and Maputo. The pinned Mozambique address pattern juxtaposes city and region tokens; the formatter inserts a comma when both are present. The pools reflect multiple naming traditions recorded among documented people and support common two-field developer forms; they are not national name-frequency estimates.

The Burundi (`BI`) and Gabon (`GA`) review adds 50 components per field from the same candidate snapshot. Given-name candidates were checked by field and ranked position. Second-name components were selected explicitly from the country-citizenship results to exclude obvious stray foreign labels and given names entered as surnames. Top, middle, and tail generated names and illustrative addresses were checked for Bujumbura and Libreville. The Burundi second field can be a personal name rather than an inherited family surname. These are two-field compatibility pools with a documented-person bias, not population-frequency samples.

La Réunion (`RE`) uses two aggregated civil-registration files from Insee: [given names by department, 2000–2025](https://www.insee.fr/fr/statistiques/8595130) (department `974`, source ZIP SHA-256 `0d69c7083016e3e4e72457d6aa43a452cbae97d09b821f0736d74c32e76cf55c`) and [family names by department, 1891–2000](https://www.insee.fr/fr/statistiques/3536630) (historic department `9D`, source ZIP SHA-256 `d46698e90bb90783f3ff99dea779a51bc8186ce375bb340fac6bb84988b87b43`). The files were grouped by name across published years or decades, sorted by summed count, and title-cased for display. The resulting pools use the top 50 female and male given names and 100 family names; the invalid spaced label `K BIDI` was excluded. Top, middle, and tail generated samples and a Saint-Denis address with its `97400` postcode were checked. Insee describes these as birth-place statistics, so the pool does not represent current residents or every age. The downloaded files contain aggregated counts, and no source-person records enter this repository. The [Insee open-data catalogue](https://www.insee.fr/fr/information/8184173) describes its open-licence terms.

The Central Africa and Horn review adds Central African Republic (`CF`), Mauritius (`MU`), Sudan (`SD`), Somalia (`SO`), and South Sudan (`SS`) from the pinned QLever candidate snapshot, with 50 female, 50 male, and 50 second-name components per code. The CF and SO second-name lists were selected explicitly from local-citizenship candidates to exclude conspicuous foreign labels. Female lists were screened for male names, full-person labels, and place names before selection. Top, middle, and tail generated names and illustrative addresses were checked for Bangui, Port Louis, Khartoum, Mogadishu, and Juba; missing postcodes remain `null`. These results are still bounded compatibility pools from documented people; they are not national frequency estimates, and some second-name components follow local patronymic rather than inherited-family conventions.

Libya (`LY`) now uses 50 female, 50 male, and 50 second-name components. Most come from the citizenship-based Wikidata snapshot; five additional female names were selected from the pinned birth-place snapshot after checking spelling and Libyan plausibility. Stray non-name and clearly unrelated second-name labels were excluded. Mayotte (`YT`) uses the [Insee department 976 given-name counts](https://www.insee.fr/fr/statistiques/8595130) for 50 female and 50 male names; 50 second-name components were selected from the birth-place snapshot and the [French Interior Ministry's 2021 elected-councillor results](https://www.archives-resultats-elections.interieur.gouv.fr/resultats/departementales-2021/976/CD976.php). Top, middle, and tail generated names and illustrative addresses were checked for Tripoli and Mamoudzou; Mayotte's `97600` postcode is linked to that city and Tripoli's unverified postcode remains `null`. The birth-place list includes people of different origins born in Mayotte, and the officeholder list is not a population sample. Neither pool asserts name frequencies for current residents or every age.

The final Africa name review opens Djibouti (`DJ`), Eritrea (`ER`), Guinea-Bissau (`GW`), Comoros (`KM`), Mauritania (`MR`), Niger (`NE`), São Tomé and Príncipe (`ST`), Eswatini (`SZ`), and Chad (`TD`). Their main components come from the pinned citizenship-based [Wikidata/QLever aggregate snapshot](https://qlever.dev/api/wikidata); a few female given names were supplemented from the separate birth-place snapshot after manual review. Obvious place labels, male names misclassified as female, malformed whole-person entries, and unrelated components were removed. Each pool has at least 18 female, 32 male, and 20 second-name components; several have 50 per field, while the smaller territories have fewer source-backed candidates. The source counts are biased toward documented people and do not represent resident name frequencies. A second display component may be a paternal name rather than an inherited surname.

Saint Helena (`SH`) uses recombined given and second-name components from the government's [2021](https://www.sainthelena.gov.sh/official-list-of-candidates-for-the-2021-general-election/) and [2025](https://www.sainthelena.gov.sh/official-list-of-candidates-for-the-2025-general-election/) adult election candidate lists. It has 16 female, 40 male, and 25 second-name components. This is an intentionally small island compatibility pool, not a population sample. Western Sahara (`EH`) uses Arabic-script name components from the [Sahrawi Women's Union's 2026 committee roster](https://spsrasd.info/ar/2026/01/26/13930.html): 23 female personal-name components and 23 male and 31 second-name components found within the published multi-part names. These components are a Sahrawi cultural sample, not a residence register or an assertion about the disputed territory's population. The second field is a recombined name component for two-field forms and need not be an inherited surname. No complete source-person record is copied into a generated profile.

| Code | Female | Male | Second | Main source |
| --- | ---: | ---: | ---: | --- |
| DJ | 18 | 32 | 20 | Wikidata citizenship |
| EH | 23 | 23 | 31 | Sahrawi Women's Union committee |
| ER | 34 | 50 | 50 | Wikidata citizenship |
| GW | 41 | 50 | 50 | Wikidata citizenship |
| KM | 19 | 50 | 50 | Wikidata citizenship |
| MR | 20 | 50 | 49 | Wikidata citizenship |
| NE | 49 | 50 | 50 | Wikidata citizenship |
| SH | 16 | 40 | 25 | St Helena election candidates |
| ST | 29 | 49 | 50 | Wikidata citizenship |
| SZ | 31 | 48 | 27 | Wikidata citizenship |
| TD | 45 | 50 | 48 | Wikidata citizenship |

Africa's 58 profile-enabled country and territory codes now receive deterministic synthetic street lines for every sampled city. Street forms are selected per country; their surname or recombined given-name and surname components come from that country's reviewed name pool. The generated line is intended to be locally plausible, not evidence that the street or building exists. The city, region, and any postal code remain tied to the sampled locality. A missing city-linked postal code stays `null`. The coverage matrix records `synthetic-street` and keeps all 58 address cells `partial`; a generated line does not turn a generic national format or missing required postal data into a verified mailing address. `reviewed` records a bounded example review, not a guarantee of local deliverability.

Europe's 50 profile-enabled codes now use the same synthetic street policy across all 588 sampled cities. Street forms follow country-level conventions, with regional choices for Flanders and Wallonia in Belgium, the German-, French-, and Italian-speaking sampled Swiss regions, and Catalonia in Spain. Country-linked reviewed name components provide the personal-name parts. Iceland uses a small project-owned pool of Icelandic street-name roots rather than combining patronymics with suffixes. The examples were checked across all 50 codes and the multilingual regions. [Deutsche Post](https://www.deutschepost.de/de/b/briefumschlag-richtig-beschriften.html), [PostNL](https://www.postnl.nl/versturen/brief-of-kaart-versturen/hoe-verstuur-ik-een-brief-of-kaart/brief-adresseren/), [Poste Italiane](https://www.poste.it/risorse/postali/pdf/standard_composizione_indirizzi.pdf), and [Swiss Post](https://www.post.ch/en/sending-letters/addressing-and-designing/addressing-consignments-correctly) provide reference examples for the line structure and building-number placement. The generated lines do not assert real roads, buildings, or deliverable postal addresses. `SJ` and `VA` retain `line1: null` while their profile-name pools remain pending.

Asia's 40 profile-enabled codes now have synthetic address lines across 482 sampled cities. Most use a locally reviewed name component with a country-level road form. Bhutan and Myanmar use small project-owned place-word pools because their profile names do not follow a universal inherited-surname pattern. China, Hong Kong, Japan, both Koreas, Taiwan, and Thailand also use place-word pools where recombining a random person's name would be less plausible as a road label. Japanese output uses a synthetic block and chome form; South Korean output uses road-name suffixes, while North Korean output uses translated street labels. [Japan Post's international address example](https://www.post.japanpost.jp/enjoy/culture/howto/navi/e-letter/contents.html), the [UPU's South Korean road-name description](https://www.upu.int/UPU/media/upu/publications/whitePaperAddressingTheWorldEn.pdf), and the [UPU's Saudi address examples](https://www.upu.int/UPU/media/upu/PostalEntitiesFiles/addressingUnit/sauEn.pdf) inform these choices. Street words and numbers remain illustrative, not deliverable or geocoded facts. The ten Asian codes still pending local name review retain `line1: null`.

The American batch adds synthetic street lines for all 31 profile-enabled North American and Caribbean codes (361 sampled cities) and all 14 South American codes (157 cities). Most use country-linked reviewed name components with English, French, Spanish, Portuguese, or Dutch road words. Montréal and other sampled Quebec cities use French road words. Aruba uses `Caya`, Curaçao uses `Kaya`, and Greenland uses a synthetic `B-` building identifier rather than inventing a street name. Spanish road words differ for Mexico, Puerto Rico, and the other sampled countries. [Canada Post's civic-address guide](https://www.canadapost-postescanada.ca/cpc/en/support/articles/addressing-guidelines/civic-address.page), [Brazil's Correios address guide](https://www.correios.com.br/enviar/precisa-de-ajuda/guia-de-enderecamento/guia-de-enderecamento), [Aruba Tourism Authority address examples](https://www.aruba.com/us/explore/mango), [Curaçao Tourism Board address examples](https://www.curacao.com/pt/event/vegan-village), and [Greenland's address registry](https://www.kaqa.gl/Anvendelse/Brugervejledning/Folkeregister/Adresseregister) informed these forms. The ten North American territory codes pending local name review retain `line1: null`.

The Oceanian batch adds synthetic street lines for all 16 profile-enabled codes and 178 sampled cities. English road forms cover the reviewed English-address examples, while New Caledonia and French Polynesia use French forms and Timor-Leste uses Portuguese forms. [Australia Post](https://auspost.com.au/personal/sending/sending-guidelines/addressing-guidelines), [NZ Post](https://www.nzpost.co.nz/personal/sending-in-nz/how-to-address-mail), the [UPU's Fiji example](https://www.upu.int/UPU/media/upu/PostalEntitiesFiles/addressingUnit/fjiEn.pdf), and [Díli's municipal address](https://dili.gov.tl/) informed the line structures. Eleven Oceanian codes remain pending local name review and retain `line1: null`. With this batch, all 209 codes currently available for profile generation receive a synthetic `line1` in every sampled city; these lines do not establish a real street, building, or deliverable address.

The street forms use country-level conventions, such as `Rue` and `Avenue` in Congo, `Rua` and `Avenida` in Mozambique, `Lalana` in Madagascar, and `Barabara ya` in Tanzania. The latter two forms were checked against [Malagasy tourism addresses](https://annuaire.tourisme.gov.mg/etablissement-dhebergement-et-de-restauration/sakamanga) and [Tanzanian government office addresses](https://www.tba.go.tz/regional_offices). The Somali `Waddada` form appears in a [government address](https://moci.gov.so/khariidadda-bogga/); Rwandan road labels use English forms in [Kigali city guidance](https://kigalicity.gov.rw/1/city-guide/leisure-in-kigali). The components remain in their local spelling; proper names are not translated. The building number is generated and could coincidentally describe a real building. This is test and mockup data, not a postal or geocoding service.

Kenya's original narrow Luo-associated sample came from the [13th National Assembly's published member list](https://parliament.go.ke/sites/default/files/2025-07/List%20of%20Members%20by%20Parties%2013th%20Parliament%20as%20at%2002072025.pdf). Namibia combines selected components from the [8th National Assembly's public member roster](https://www.parliament.na/8th-national-assembly-2025-2030/). The Namibian pool remains a small form-compatible example, not a population-frequency distribution or a copy of complete member records.

Tanzania's initial sample used name components from the [Parliament's member profiles](https://polis.parliament.go.tz/members), which identify member sex. Zambia's initial sample used the [National Assembly's public members list](https://www.parliament.gov.zm/members-of-parliament), and Zimbabwe's used the [Parliament's public ministers list](https://www.parlzim.gov.zw/ministers/). These sources remain supplementary to the expanded pools and do not establish population or age distributions.

Kenya, Tanzania, Uganda, Zambia, and Zimbabwe now use the pinned QLever candidate snapshot for their wider pools, with their earlier reviewed components retained. Their female/male/second-name pool sizes are respectively `53/52/54`, `55/52/58`, `52/51/58`, `56/53/56`, and `53/53/56`. The selected entries were reviewed across the top, middle, and tail of the candidate rankings, with obvious field errors removed. Some second-name components can also be given names under local naming conventions. The broader lists still reflect Wikidata's documented-person bias; they are compatibility data, not national frequencies.

Botswana uses components from the [Parliament's public member roster](https://parliament.gov.bw/members.php). Lesotho uses selected components from an [official Assembly member listing](https://nationalassembly.parliament.ls/members/); this source includes earlier mandates and does not establish name frequencies. Seychelles uses the [8th Assembly roster](https://www.nationalassembly.sc/members-8th-assembly), with the [Women's Parliamentary Caucus list](https://www.nationalassembly.sc/committees/standing-committees/womens-parliamentary-caucus) supporting the female examples. As with the other parliamentary samples, combining name components is only a narrow compatibility choice and does not assert demographic coverage.

Unfiltered country and appearance choices currently give each profile-available code and each appearance label equal editorial weight. Within a country, city choice uses the square root of the GeoNames population, rounded to a minimum weight of one. Country-to-name-pool choices use the language weights above. All choices use component keys derived from `seed`, `asOf`, the filters, and data versions; explicit country, city, and appearance filters bypass weighted selection. These weights are not population or ancestry estimates. The distribution coverage cell stays `partial` until country and appearance defaults are reviewed for the public beta. An explicit country must be profile-available; appearance selection remains independent.

## Fictional addresses

Country address order and required fields come from [Google's Address Data Service metadata](https://github.com/google/libaddressinput/wiki/AddressValidationMetadata), licensed [CC BY 4.0](https://github.com/google/libaddressinput) for the data. Credit: **Google**. The 2026-09-24 snapshot contains all 249 assigned codes; 195 have an explicit country format, and 54 use the documented generic format. Run `npm run data:fetch:address-metadata -- <output.json>` to fetch a new source copy, then review its hash and license before running `npm run data:refresh:addresses -- <output.json> <postal-allCountries.zip>`. API requests use the checked-in snapshot only.

City-linked postal examples come from [GeoNames' postal extract](https://download.geonames.org/export/zip/) (CC BY 4.0; credit: **GeoNames**). The build keeps a full-format postal code only when its place name matches a sampled city and its coordinates are within 25 km of that city. This currently yields 553 sampled cities across 78 country codes. It never borrows a code from a different city or fabricates one from a country-wide pattern. GeoNames notes that its postal extract covers roughly 100 countries and includes partial codes in some places; partial codes are rejected by the country format check. Address coverage remains `partial` where streets are synthetic or unavailable; missing required codes or regions and generic formats are also recorded.

For every profile-enabled country, the API composes a synthetic `line1` from a local street or building form and country-linked name or place components. Codes pending profile-name review retain `line1: null`. The structured address includes nullable `line1`, `city`, nullable `region`, nullable `postalCode`, `country`, and `formatted` in the country's published field order. It is intended for mockups and tests, not delivery or geocoding. A nullable postal code signals missing city-linked evidence, including in countries where a deliverable address would normally require one. The coverage matrix makes that limitation visible rather than returning a mismatched value.

Pitcairn is an explicit exception: the [Universal Postal Union](https://www.upu.int/UPU/media/upu/PostalEntitiesFiles/addressingUnit/pcnEn.pdf) documents `PCRN 1ZZ` as the single postcode for the territory. Its published postcode is retained while `line1` remains `null`. The exception is recorded separately from the GeoNames postal extract so it is not presented as a city match found there.

## Catalog validation and audit

The refresh scripts check pinned source hashes before generating snapshots. ISO records are unique by code. The city build keeps one entry per case-folded name and country, retaining the more populous candidate in its bounded sample. Name arrays remove exact duplicate strings before even sampling. Postal examples are keyed by GeoNames city ID and retain the nearest full-format match within 25 km. Every ingested coverage cell has a licensed source in the manifest; additional inputs such as language tags, administrative names, postal codes, and the UPU Pitcairn exception are listed separately.

Run `npm run data:audit` for a concise status or `npm run data:audit -- --json` for every per-code gap. The audit checks all 249 registry codes, generates controlled male and female geographic/contact samples for all 242 resident-eligible codes, validates city/country relationships and address output, and fails on malformed samples or missing source rights. It lists partial data and review work without marking machine-checked names or addresses as culturally reviewed. The HTTP and portrait audit is tracked in the public-beta Epic after those components exist.

The audit and the later response metadata use `geographicDataVersion`, separately from the ISO registry edition used as the audit's fixed `asOf` date. Bump the geographic data version whenever a snapshot or its generation policy changes, so a seeded response cannot silently reuse a version label for different data.

## Fictional telephone policy

Telephone output is optional. A calling code is metadata for validation and presentation; it is never sufficient to invent a safe subscriber number. A country receives generated telephone numbers only after a country-specific, officially reserved fictional/test range has been reviewed and versioned. Otherwise `phone` is `null`. The API must not suggest that a format-valid random number is unassigned, reachable, or safe to call.

[NANPA's reserved 555-0100 through 555-0199 block](https://nanpa.com/numbering/555-line-numbers) and [Ofcom's UK drama ranges](https://www.ofcom.org.uk/phones-and-broadband/phone-numbers/numbers-for-drama) provide the first reviewed ranges. Each batch must tie a reserved range to the correct national or area prefix and test an E.164-formatted output. No arbitrary subscriber numbers will be emitted for other countries.

The phone catalog uses [NANPA's non-working 555-0100 through 555-0199 range](https://nanpa.com/numbering/555-line-numbers) with [area codes](https://www.nanpa.com/resources/area-code-maps) for the sampled US cities; [Ofcom's UK-wide mobile drama range](https://www.ofcom.org.uk/phones-and-broadband/phone-numbers/numbers-for-drama) `07700 900000` through `07700 900999` for GB; [ACMA's geographic fictional ranges](https://www.acma.gov.au/phone-numbers-use-tv-shows-films-and-creative-works) for the sampled Australian cities; and [CRTC's reserved 555 range](https://crtc.gc.ca/eng/archive/2017/2017-203.htm) with [Canadian local area codes](https://cnac.ca/reference_data/canadian_exchange_area_listing.htm). Values are emitted in E.164 form and selected from a stable generation key. The coverage matrix marks full sampled-city phone coverage for US, CA, AU, and GB; it does not claim that every city in those countries is in the bounded city sample. Other country codes currently return `phone: null`. New ranges require official allocation evidence and city or nationwide applicability before use.

Email values are generated under the reserved `example.test` domain. Accented Latin names are folded for the local part; a stable key suffix prevents common-name collisions within a response. For scripts that do not fold to ASCII, the local part falls back to `person`. The contact fields are illustrative and must never be used to deliver mail or place calls.
